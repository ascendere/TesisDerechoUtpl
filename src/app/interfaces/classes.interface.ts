export default interface Class {
  id: string;
  subjectName: string;
  parallel: string;
  cicleId: String;
  type: string;
  professorId: string;
  directorName?: string;
  description?: string;
}
